# .github/workflows/auto-assign.yml
name: "Auto Assign"

on:
  pull_request:
    types: [opened]

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v2.0.0
        with:
          repo-token: "${{ secrets.AUTORELEASE_PAT }}"
